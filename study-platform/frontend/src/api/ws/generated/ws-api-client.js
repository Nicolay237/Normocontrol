// @ts-nocheck
// Generated by wsapi 0.1.0. Do not edit.

/**
 * @typedef {Object} UserInvitationInput
 * @property {ResourceType} resource_type
 * @property {string} resource_id
 * @property {string} user_id
 * @property {Role} role
 */

/**
 * @typedef {Object} UserInvitationOutput
 * @property {ResourceType} resource_type
 * @property {string} resource_id
 * @property {string} user_id
 * @property {string} personal_group_id
 * @property {Role} role
 * @property {boolean} active
 */

/**
 * @typedef {Object} MessageDeleteInput
 * @property {string} message_id
 */

/**
 * @typedef {Object} MessageDeleted
 * @property {string} chat_id
 * @property {string} message_id
 */

/**
 * @typedef {Object} MessageEditInput
 * @property {string} message_id
 * @property {string} text
 */

/**
 * @typedef {Object} MessageOutput
 * @property {string} id
 * @property {string} chat_id
 * @property {string} author_id
 * @property {string} text
 * @property {string|null} block_id
 * @property {string|null} revision_id
 * @property {string|null} parent_message_id
 * @property {string} created_at
 * @property {string|null} edited_at
 * @property {string|null} deleted_at
 */

/**
 * @typedef {Object} ChatHistoryInput
 * @property {string} chat_id
 * @property {number} [limit]
 * @property {string|null} [before_message_id]
 */

/**
 * @typedef {Object} ChatOutput
 * @property {string} chat_id
 * @property {Array<MessageOutput>} messages
 * @property {boolean} [has_more]
 * @property {string|null} [next_before_message_id]
 */

/**
 * @typedef {Object} ChatOpenInput
 * @property {string} resource_type
 * @property {string} resource_id
 */

/**
 * @typedef {Object} MessageSendInput
 * @property {string} chat_id
 * @property {string} text
 * @property {string|null} [block_id]
 * @property {string|null} [revision_id]
 * @property {string|null} [parent_message_id]
 */

/**
 * @typedef {Object} FolderAccessListInput
 * @property {string} folder_id
 */

/**
 * @typedef {Object} FolderAccessOutput
 * @property {string} folder_id
 * @property {Array<RoleGroupConditionsOutput>} roles
 */

/**
 * @typedef {Object} FolderAccessRemoveInput
 * @property {string} folder_id
 * @property {Role} role
 */

/**
 * @typedef {Object} FolderAccessSetInput
 * @property {string} folder_id
 * @property {Role} role
 * @property {Array<Array<string>>} group_sets
 */

/**
 * @typedef {Object} FolderCreateInput
 * @property {string} parent_id
 * @property {string} name
 * @property {string|null} [description]
 * @property {string|null} [icon_name]
 * @property {string|null} [order_key]
 */

/**
 * @typedef {Object} FolderOutput
 * @property {string} id
 * @property {string|null} parent_id
 * @property {string} kind
 * @property {string} name
 * @property {string|null} description
 * @property {string|null} icon_name
 * @property {string|null} target_folder_id
 * @property {string} order_key
 */

/**
 * @typedef {Object} FolderDeleteInput
 * @property {string} folder_id
 */

/**
 * @typedef {Object} FolderDeleteOutput
 * @property {string} folder_id
 * @property {boolean} deleted
 */

/**
 * @typedef {Object} FolderMoveInput
 * @property {string} folder_id
 * @property {string} parent_id
 * @property {string|null} [order_key]
 */

/**
 * @typedef {Object} FolderOpenInput
 * @property {string} folder_id
 */

/**
 * @typedef {Object} FolderOpenOutput
 * @property {FolderOutput} folder
 * @property {Array<FolderOutput>} breadcrumbs
 * @property {Role} effective_role
 * @property {Array<FolderChild>} children
 * @property {string|null} [redirect_target_id]
 */

/**
 * @typedef {Object} FolderOwnerTransferInput
 * @property {string} folder_id
 * @property {string} target_group_id
 */

/**
 * @typedef {Object} FolderRestoreInput
 * @property {string} folder_id
 */

/**
 * @typedef {Object} FolderShortcutCreateInput
 * @property {string} parent_id
 * @property {string} target_folder_id
 * @property {string} name
 * @property {string|null} [description]
 * @property {string|null} [icon_name]
 * @property {string|null} [order_key]
 */

/**
 * @typedef {Object} FolderShortcutUpdateInput
 * @property {string} folder_id
 * @property {string|null} [target_folder_id]
 * @property {string|null} [name]
 * @property {string|null} [description]
 * @property {string|null} [icon_name]
 * @property {string|null} [order_key]
 */

/**
 * @typedef {Object} FolderUpdateInput
 * @property {string} folder_id
 * @property {string|null} [name]
 * @property {string|null} [description]
 * @property {string|null} [icon_name]
 * @property {string|null} [order_key]
 */

/**
 * @typedef {Object} LabPageInput
 * @property {string} page_id
 */

/**
 * @typedef {Object} LabSubmissionOutput
 * @property {string} id
 * @property {string} page_id
 * @property {string} user_id
 * @property {number} version
 * @property {"draft"|"submitted"|"returned"|"graded"} status
 * @property {string|null} comment
 * @property {string|null} submitted_at
 * @property {boolean} is_late
 * @property {number|string|null} score
 * @property {boolean|null} passed
 * @property {string|null} feedback
 * @property {string|null} graded_at
 * @property {SourceType} [source_type]
 * @property {Array<LabFileOutput>} files
 * @property {LabGitSnapshotOutput|null} [git_snapshot]
 */

/**
 * @typedef {Object} LabGitSelectionInput
 * @property {string} page_id
 * @property {string} instance_id
 * @property {string} repository_full_name
 * @property {string|null} [repository_external_id]
 * @property {string|null} [branch]
 * @property {number|null} [change_request_number]
 */

/**
 * @typedef {Object} LabGitPreviewOutput
 * @property {string} page_id
 * @property {string} instance_id
 * @property {ProviderType} provider_type
 * @property {string} repository_full_name
 * @property {string|null} repository_web_url
 * @property {string} branch
 * @property {string} commit_sha
 * @property {string} commit_short_sha
 * @property {string} commit_message
 * @property {string|null} commit_author
 * @property {string|null} commit_author_login
 * @property {string|null} commit_author_avatar_url
 * @property {string|null} commit_authored_at
 * @property {string|null} commit_web_url
 * @property {Array<ChangedFileSummary>|null} commit_changed_files
 * @property {number|null} commit_changed_files_total
 * @property {boolean} commit_changed_files_truncated
 * @property {number|null} commit_additions
 * @property {number|null} commit_deletions
 * @property {number|null} change_request_number
 * @property {string|null} change_request_title
 * @property {ChangeRequestStatus|null} change_request_status
 * @property {string|null} change_request_url
 * @property {string|null} change_request_target_branch
 * @property {CiStatus|null} ci_status
 * @property {string|null} ci_url
 * @property {boolean} is_late
 */

/**
 * @typedef {Object} LabGitSubmitInput
 * @property {string} page_id
 * @property {string} instance_id
 * @property {string} repository_full_name
 * @property {string|null} [repository_external_id]
 * @property {string|null} [branch]
 * @property {number|null} [change_request_number]
 * @property {string|null} [comment]
 */

/**
 * @typedef {Object} LabStudentViewOutput
 * @property {LabSettingsOutput} settings
 * @property {Array<LabSubmissionOutput>} submissions
 * @property {boolean} can_submit
 * @property {string|null} blocked_reason
 */

/**
 * @typedef {Object} LabRosterOutput
 * @property {LabSettingsOutput} settings
 * @property {Array<LabRosterEntry>} entries
 */

/**
 * @typedef {Object} LabSettingsOutput
 * @property {string} page_id
 * @property {string|null} due_at
 * @property {"score"|"pass_fail"} grading
 * @property {number|string} max_score
 * @property {string|null} instructions
 * @property {SubmissionMode} submission_mode
 * @property {Array<string>} allowed_source_control_instance_ids
 * @property {boolean} require_change_request
 * @property {string|null} target_branch
 * @property {boolean} [configured]
 */

/**
 * @typedef {Object} LabSettingsInput
 * @property {string} page_id
 * @property {string|null} [due_at]
 * @property {"score"|"pass_fail"} [grading]
 * @property {number|string} [max_score]
 * @property {string|null} [instructions]
 * @property {SubmissionMode} [submission_mode]
 * @property {Array<string>} [allowed_source_control_instance_ids]
 * @property {boolean} [require_change_request]
 * @property {string|null} [target_branch]
 */

/**
 * @typedef {Object} LabGradeInput
 * @property {string} submission_id
 * @property {number|string|null} [score]
 * @property {boolean|null} [passed]
 * @property {string|null} [feedback]
 * @property {boolean} [regrade]
 */

/**
 * @typedef {Object} LabReturnInput
 * @property {string} submission_id
 * @property {string|null} [feedback]
 */

/**
 * @typedef {Object} LabSubmissionHistoryInput
 * @property {string} page_id
 * @property {string} user_id
 */

/**
 * @typedef {Object} LabSubmissionHistoryOutput
 * @property {Array<LabSubmissionOutput>} submissions
 */

/**
 * @typedef {Object} LabSubmitInput
 * @property {string} page_id
 * @property {Array<string>} file_ids
 * @property {string|null} [comment]
 */

/**
 * @typedef {Object} NormocontrolPageInput
 * @property {string} page_id
 */

/**
 * @typedef {Object} NormocontrolAuthorOutput
 * @property {string} id
 * @property {string} username
 * @property {string} display_name
 * @property {string|null} [avatar_id]
 */

/**
 * @typedef {Object} NormocontrolViewOutput
 * @property {NormocontrolSettingsOutput} settings
 * @property {Array<NormocontrolRunOutput>} runs
 * @property {boolean} service_available
 * @property {number} pending
 * @property {boolean} [reviewing_all]
 */

/**
 * @typedef {Object} NormocontrolRunIdInput
 * @property {string} run_id
 */

/**
 * @typedef {Object} NormocontrolRunOutput
 * @property {string} id
 * @property {string} page_id
 * @property {string} user_id
 * @property {RunStatus} status
 * @property {string|null} original_name
 * @property {number|null} size
 * @property {number|null} [queue_position]
 * @property {FailureReason|null} [error_code]
 * @property {string} created_at
 * @property {string|null} finished_at
 * @property {NormocontrolReportOutput|null} [report]
 * @property {NormocontrolAuthorOutput|null} [author]
 */

/**
 * @typedef {Object} NormocontrolSubmitInput
 * @property {string} run_id
 * @property {string} file_id
 */

/**
 * @typedef {Object} NormocontrolSettingsOutput
 * @property {string} page_id
 * @property {string|null} instructions
 * @property {boolean} accepting
 * @property {boolean} [configured]
 * @property {Array<string>} allowed_extensions
 * @property {number} max_file_size
 */

/**
 * @typedef {Object} NormocontrolSettingsInput
 * @property {string} page_id
 * @property {string|null} [instructions]
 * @property {boolean} [accepting]
 */

/**
 * @typedef {Object} PageAccessListInput
 * @property {string} page_id
 */

/**
 * @typedef {Object} PageAccessOutput
 * @property {string} page_id
 * @property {Array<PageRoleGroupConditionsOutput>} roles
 */

/**
 * @typedef {Object} PageAccessRemoveInput
 * @property {string} page_id
 * @property {Role} role
 */

/**
 * @typedef {Object} PageAccessSetInput
 * @property {string} page_id
 * @property {Role} role
 * @property {Array<Array<string>>} group_sets
 */

/**
 * @typedef {Object} BlockMoveInput
 * @property {string} block_id
 * @property {string} lease_id
 * @property {number} expected_block_version
 * @property {string} zone
 * @property {string} order_key
 */

/**
 * @typedef {Object} BlockOutput
 * @property {string} id
 * @property {string} page_id
 * @property {string} block_type
 * @property {number} schema_version
 * @property {string} zone
 * @property {string} order_key
 * @property {string} ownership
 * @property {*} data
 * @property {number} block_version
 * @property {object|null} [computed_data]
 */

/**
 * @typedef {Object} BlockCreateInput
 * @property {string} page_id
 * @property {string} block_type
 * @property {string} [zone]
 * @property {string|null} [order_key]
 * @property {*} [data]
 */

/**
 * @typedef {Object} BlockDeleteInput
 * @property {string} block_id
 * @property {string} lease_id
 * @property {number} expected_block_version
 */

/**
 * @typedef {Object} BlockDeleteOutput
 * @property {string} block_id
 * @property {boolean} deleted
 */

/**
 * @typedef {Object} BlockLockInput
 * @property {string} block_id
 */

/**
 * @typedef {Object} BlockLeaseOutput
 * @property {string} block_id
 * @property {string} lease_id
 * @property {string} user_id
 * @property {string} session_id
 * @property {string} expires_at
 */

/**
 * @typedef {Object} BlockLeaseInput
 * @property {string} block_id
 * @property {string} lease_id
 */

/**
 * @typedef {Object} BlockLockChanged
 * @property {string} block_id
 * @property {BlockLeaseOutput|null} lease
 */

/**
 * @typedef {Object} BlockLockSwitchInput
 * @property {string} current_block_id
 * @property {string} current_lease_id
 * @property {string} next_block_id
 */

/**
 * @typedef {Object} BlockTypeChangeInput
 * @property {string} block_id
 * @property {string} lease_id
 * @property {number} expected_block_version
 * @property {string} block_type
 * @property {*} [data]
 */

/**
 * @typedef {Object} BlockUpdateInput
 * @property {string} block_id
 * @property {string} lease_id
 * @property {number} expected_block_version
 * @property {*} data
 */

/**
 * @typedef {Object} PageCreateInput
 * @property {string} folder_id
 * @property {"document"|"lecture"|"resource"|"lab"|"normocontrol"} [kind]
 * @property {string} name
 * @property {string|null} [description]
 * @property {string|null} [icon_name]
 * @property {string|null} [order_key]
 */

/**
 * @typedef {Object} PageOutput
 * @property {string} id
 * @property {string} folder_id
 * @property {string} kind
 * @property {string} name
 * @property {string|null} description
 * @property {string|null} icon_name
 * @property {string} order_key
 * @property {number} draft_head_sequence
 * @property {string|null} latest_revision_id
 * @property {string|null} published_revision_id
 * @property {string|null} [created_by]
 * @property {PageAuthorOutput|null} [author]
 */

/**
 * @typedef {Object} PageDeleteInput
 * @property {string} page_id
 */

/**
 * @typedef {Object} PageDeleteOutput
 * @property {string} page_id
 * @property {boolean} deleted
 */

/**
 * @typedef {Object} PageInputEventsAppendInput
 * @property {string} page_id
 * @property {Array<PageInputEventItem>} events
 */

/**
 * @typedef {Object} PageInputEventsAppendOutput
 * @property {number} accepted
 */

/**
 * @typedef {Object} PageOpenInput
 * @property {string} page_id
 */

/**
 * @typedef {Object} PageOpenOutput
 * @property {PageOutput} page
 * @property {Role} effective_role
 * @property {Array<BlockOutput>} blocks
 * @property {Array<PageResourceOutput>} [resources]
 */

/**
 * @typedef {Object} PageOwnerTransferInput
 * @property {string} page_id
 * @property {string} target_group_id
 */

/**
 * @typedef {Object} ProvenanceMarkInput
 * @property {string} page_id
 * @property {string} block_id
 * @property {"original"|"citation"|"paraphrase"|"ai_generated"|"imported"|"unknown"} kind
 * @property {string|null} [source_id]
 * @property {string|null} [selected_text]
 * @property {number|null} [start_offset]
 * @property {number|null} [end_offset]
 */

/**
 * @typedef {Object} ProvenanceOutput
 * @property {string} id
 * @property {string} page_id
 * @property {string} block_id
 * @property {string} kind
 * @property {string|null} source_id
 * @property {string} text_hash
 * @property {string|null} selected_text
 * @property {number|null} start_offset
 * @property {number|null} end_offset
 * @property {string} created_by
 * @property {string} created_at
 */

/**
 * @typedef {Object} ProvenanceRemoveInput
 * @property {string} provenance_id
 */

/**
 * @typedef {Object} ProvenanceRemoveOutput
 * @property {string} provenance_id
 * @property {boolean} [removed]
 */

/**
 * @typedef {Object} ProvenanceReportInput
 * @property {string} page_id
 * @property {string} revision_id
 */

/**
 * @typedef {Object} ProvenanceReportOutput
 * @property {string} id
 * @property {string} page_id
 * @property {string} revision_id
 * @property {object} result
 * @property {string} created_by
 * @property {string} created_at
 */

/**
 * @typedef {Object} PagePublishInput
 * @property {string} page_id
 */

/**
 * @typedef {Object} RevisionOutput
 * @property {string} id
 * @property {string} page_id
 * @property {number} event_sequence
 * @property {string} kind
 * @property {string|null} name
 * @property {object} snapshot
 * @property {string} created_by
 * @property {string} created_at
 */

/**
 * @typedef {Object} PageRestoreInput
 * @property {string} page_id
 */

/**
 * @typedef {Object} RevisionCreateInput
 * @property {string} page_id
 * @property {string|null} [name]
 */

/**
 * @typedef {Object} RevisionGetInput
 * @property {string} revision_id
 */

/**
 * @typedef {Object} RevisionListInput
 * @property {string} page_id
 */

/**
 * @typedef {Object} RevisionListOutput
 * @property {Array<RevisionOutput>} revisions
 */

/**
 * @typedef {Object} RevisionRestoreInput
 * @property {string} revision_id
 */

/**
 * @typedef {Object} RevisionShareCreateInput
 * @property {string} page_id
 * @property {string} revision_id
 * @property {boolean} [require_authentication]
 * @property {string|null} [expires_at]
 */

/**
 * @typedef {Object} RevisionShareCreatedOutput
 * @property {string} id
 * @property {string} page_id
 * @property {string} revision_id
 * @property {boolean} require_authentication
 * @property {string|null} expires_at
 * @property {string|null} disabled_at
 * @property {string} created_by
 * @property {string} created_at
 * @property {string} updated_at
 * @property {string} token
 */

/**
 * @typedef {Object} RevisionShareListInput
 * @property {string} page_id
 */

/**
 * @typedef {Object} RevisionShareListOutput
 * @property {Array<RevisionShareOutput>} links
 */

/**
 * @typedef {Object} RevisionShareRevokeInput
 * @property {string} share_id
 */

/**
 * @typedef {Object} RevisionShareOutput
 * @property {string} id
 * @property {string} page_id
 * @property {string} revision_id
 * @property {boolean} require_authentication
 * @property {string|null} expires_at
 * @property {string|null} disabled_at
 * @property {string} created_by
 * @property {string} created_at
 * @property {string} updated_at
 */

/**
 * @typedef {Object} RevisionShareUpdateInput
 * @property {string} share_id
 * @property {string|null} [revision_id]
 * @property {boolean|null} [require_authentication]
 * @property {string|null} [expires_at]
 */

/**
 * @typedef {Object} SourceCreateInput
 * @property {string} page_id
 * @property {string} kind
 * @property {string} title
 * @property {Array<object>} [authors]
 * @property {string|null} [url]
 * @property {string|null} [published_at]
 * @property {object} [raw_data]
 */

/**
 * @typedef {Object} SourceOutput
 * @property {string} id
 * @property {string} page_id
 * @property {string} kind
 * @property {string} title
 * @property {Array<object>} authors
 * @property {string|null} url
 * @property {string|null} published_at
 * @property {object} raw_data
 * @property {string} created_by
 * @property {string} created_at
 */

/**
 * @typedef {Object} SourceDeleteInput
 * @property {string} source_id
 */

/**
 * @typedef {Object} SourceDeleteOutput
 * @property {string} source_id
 * @property {boolean} [deleted]
 */

/**
 * @typedef {Object} SourceListInput
 * @property {string} page_id
 */

/**
 * @typedef {Object} SourceListOutput
 * @property {Array<SourceOutput>} sources
 */

/**
 * @typedef {Object} SourceUpdateInput
 * @property {string} source_id
 * @property {string|null} [kind]
 * @property {string|null} [title]
 * @property {Array<object>|null} [authors]
 * @property {string|null} [url]
 * @property {string|null} [published_at]
 * @property {object|null} [raw_data]
 */

/**
 * @typedef {Object} PageTransactionInput
 * @property {string} page_id
 * @property {Array<BlockOpInput>} ops
 */

/**
 * @typedef {Object} PageTransactionOutput
 * @property {Array<BlockOutput>} blocks
 * @property {Array<string>} deleted
 */

/**
 * @typedef {Object} PageUpdateInput
 * @property {string} page_id
 * @property {string|null} [folder_id]
 * @property {string|null} [name]
 * @property {string|null} [description]
 * @property {string|null} [icon_name]
 * @property {string|null} [order_key]
 */

/**
 * @typedef {Object} QuestionCreateInput
 * @property {string} bank_id
 */

/**
 * @typedef {Object} QuestionOutput
 * @property {string} id
 * @property {string} bank_id
 * @property {string|null} current_draft_version_id
 * @property {string} created_by
 */

/**
 * @typedef {Object} QuestionOpenInput
 * @property {string} question_id
 */

/**
 * @typedef {Object} QuestionEditorOutput
 * @property {QuestionOutput} question
 * @property {QuestionVersionOutput|null} draft
 */

/**
 * @typedef {Object} QuestionVersionIdInput
 * @property {string} question_version_id
 */

/**
 * @typedef {Object} QuestionVersionOutput
 * @property {string} id
 * @property {string} question_id
 * @property {object} content_snapshot
 * @property {string} answer_type
 * @property {object} answer_schema
 * @property {object} answer_key
 * @property {object} scoring_rules
 * @property {string} created_by
 * @property {string} created_at
 */

/**
 * @typedef {Object} QuestionSaveInput
 * @property {string} question_id
 * @property {object} content_snapshot
 * @property {string} answer_type
 * @property {object} answer_key
 * @property {object} [scoring_rules]
 */

/**
 * @typedef {Object} QuestionBankCreateInput
 * @property {string} owner_folder_id
 * @property {string} name
 * @property {string|null} [description]
 */

/**
 * @typedef {Object} QuestionBankOutput
 * @property {string} id
 * @property {string} owner_folder_id
 * @property {string} name
 * @property {string|null} description
 * @property {string} created_by
 * @property {string} created_at
 */

/**
 * @typedef {Object} QuestionBankIdInput
 * @property {string} bank_id
 */

/**
 * @typedef {Object} DeletedOutput
 * @property {string} id
 * @property {boolean} [deleted]
 */

/**
 * @typedef {Object} QuestionBankListInput
 * @property {string} owner_folder_id
 */

/**
 * @typedef {Object} QuestionBankListOutput
 * @property {Array<QuestionBankOutput>} banks
 */

/**
 * @typedef {Object} QuestionBankUpdateInput
 * @property {string} bank_id
 * @property {string|null} [name]
 * @property {string|null} [description]
 */

/**
 * @typedef {Object} ConnectionDisconnectInput
 * @property {string} connection_id
 */

/**
 * @typedef {Object} ConnectionDisconnectOutput
 * @property {string} connection_id
 * @property {boolean} [disconnected]
 */

/**
 * @typedef {Object} InstanceListInput
 * @property {boolean} [include_disabled]
 */

/**
 * @typedef {Object} ConnectionListOutput
 * @property {Array<SourceControlConnectionOutput>} connections
 */

/**
 * @typedef {Object} InstanceListOutput
 * @property {Array<SourceControlInstanceOutput>} instances
 */

/**
 * @typedef {Object} RepositoryListInput
 * @property {number} [page]
 * @property {number} [per_page]
 * @property {string} instance_id
 * @property {string|null} [query]
 */

/**
 * @typedef {Object} RepositoryPageOutput
 * @property {Array<RepositorySummary>} items
 * @property {number} page
 * @property {number} per_page
 * @property {boolean} has_more
 */

/**
 * @typedef {Object} RepositoryBranchesInput
 * @property {number} [page]
 * @property {number} [per_page]
 * @property {string} instance_id
 * @property {string} repository_full_name
 * @property {string|null} [repository_external_id]
 */

/**
 * @typedef {Object} BranchPageOutput
 * @property {Array<BranchSummary>} items
 * @property {number} page
 * @property {number} per_page
 * @property {boolean} has_more
 */

/**
 * @typedef {Object} ChangeRequestGetInput
 * @property {string} instance_id
 * @property {string} repository_full_name
 * @property {string|null} [repository_external_id]
 * @property {number} number
 */

/**
 * @typedef {Object} ChangeRequestSummary
 * @property {string} external_id
 * @property {number} number
 * @property {string} title
 * @property {ChangeRequestStatus} status
 * @property {string} source_branch
 * @property {string} target_branch
 * @property {string} head_sha
 * @property {string|null} [web_url]
 * @property {string|null} [author_name]
 * @property {string|null} [updated_at]
 */

/**
 * @typedef {Object} ChangeRequestListInput
 * @property {number} [page]
 * @property {number} [per_page]
 * @property {string} instance_id
 * @property {string} repository_full_name
 * @property {string|null} [repository_external_id]
 */

/**
 * @typedef {Object} ChangeRequestPageOutput
 * @property {Array<ChangeRequestSummary>} items
 * @property {number} page
 * @property {number} per_page
 * @property {boolean} has_more
 */

/**
 * @typedef {Object} CiStatusInput
 * @property {string} instance_id
 * @property {string} repository_full_name
 * @property {string|null} [repository_external_id]
 * @property {string} commit_sha
 */

/**
 * @typedef {Object} CheckSummary
 * @property {CiStatus} status
 * @property {string|null} [web_url]
 * @property {string|null} [updated_at]
 * @property {string|null} [summary]
 */

/**
 * @typedef {Object} RepositoryCommitInput
 * @property {string} instance_id
 * @property {string} repository_full_name
 * @property {string|null} [repository_external_id]
 * @property {string|null} [branch]
 * @property {string|null} [commit_sha]
 */

/**
 * @typedef {Object} CommitSummary
 * @property {string} sha
 * @property {string} short_sha
 * @property {string} message
 * @property {string|null} [author_name]
 * @property {string|null} [author_login]
 * @property {string|null} [author_avatar_url]
 * @property {string|null} [authored_at]
 * @property {string|null} [web_url]
 * @property {Array<ChangedFileSummary>|null} [changed_files]
 * @property {number|null} [changed_files_total]
 * @property {boolean} [changed_files_truncated]
 * @property {number|null} [additions]
 * @property {number|null} [deletions]
 */

/**
 * @typedef {Object} GroupCreateInput
 * @property {string} name
 * @property {string|null} [description]
 * @property {Array<string>} [capabilities]
 */

/**
 * @typedef {Object} GroupOutput
 * @property {string} id
 * @property {string} name
 * @property {string|null} description
 * @property {boolean} is_system
 * @property {boolean} is_protected
 * @property {boolean} is_personal
 * @property {string|null} personal_user_id
 * @property {Array<string>} [capabilities]
 */

/**
 * @typedef {Object} GroupIdInput
 * @property {string} group_id
 */

/**
 * @typedef {Object} GroupDeletedOutput
 * @property {string} group_id
 * @property {boolean} [deleted]
 */

/**
 * @typedef {Object} GroupSearchInput
 * @property {string} [query]
 * @property {number} [limit]
 */

/**
 * @typedef {Object} GroupSearchOutput
 * @property {Array<GroupOutput>} groups
 */

/**
 * @typedef {Object} GroupUpdateInput
 * @property {string} group_id
 * @property {string|null} [name]
 * @property {string|null} [description]
 * @property {Array<string>|null} [capabilities]
 */

/**
 * @typedef {Object} MembershipChangeInput
 * @property {string} group_id
 * @property {string} user_id
 */

/**
 * @typedef {Object} MembershipOutput
 * @property {string} group_id
 * @property {Array<string>} user_ids
 */

/**
 * @typedef {Object} MembershipBulkSetInput
 * @property {string} group_id
 * @property {Array<string>} user_ids
 */

/**
 * @typedef {Object} MembershipListInput
 * @property {string} group_id
 */

/**
 * @typedef {Object} InstanceCreateInput
 * @property {string} display_name
 * @property {ProviderType} provider_type
 * @property {string} web_base_url
 * @property {string|null} [api_base_url]
 * @property {string|null} [oauth_authorize_url]
 * @property {string|null} [oauth_token_url]
 * @property {string|null} [oauth_client_id]
 * @property {string|null} [oauth_client_secret]
 * @property {Array<string>} [oauth_scopes]
 * @property {Array<CloneProtocol>} [allowed_clone_protocols]
 * @property {object} [config]
 * @property {boolean} [enabled]
 */

/**
 * @typedef {Object} SourceControlInstanceAdminOutput
 * @property {string} id
 * @property {string} display_name
 * @property {ProviderType} provider_type
 * @property {string} web_base_url
 * @property {Array<Capability>} capabilities
 * @property {Array<CloneProtocol>} allowed_clone_protocols
 * @property {boolean} enabled
 * @property {ConnectionStatus} connection_status
 * @property {string|null} api_base_url
 * @property {string|null} oauth_authorize_url
 * @property {string|null} oauth_token_url
 * @property {string|null} oauth_client_id
 * @property {boolean} has_oauth_client_secret
 * @property {Array<string>} oauth_scopes
 * @property {object} config
 * @property {boolean} is_system
 * @property {string} created_at
 * @property {string} updated_at
 */

/**
 * @typedef {Object} InstanceDisableInput
 * @property {string} instance_id
 */

/**
 * @typedef {Object} InstanceAdminListOutput
 * @property {Array<SourceControlInstanceAdminOutput>} instances
 */

/**
 * @typedef {Object} InstanceUpdateInput
 * @property {string} instance_id
 * @property {string|null} [display_name]
 * @property {string|null} [web_base_url]
 * @property {string|null} [api_base_url]
 * @property {string|null} [oauth_authorize_url]
 * @property {string|null} [oauth_token_url]
 * @property {string|null} [oauth_client_id]
 * @property {string|null} [oauth_client_secret]
 * @property {Array<string>|null} [oauth_scopes]
 * @property {Array<CloneProtocol>|null} [allowed_clone_protocols]
 * @property {object|null} [config]
 * @property {boolean|null} [enabled]
 */

/**
 * @typedef {Object} UserCreateInput
 * @property {string} username
 * @property {string} display_name
 * @property {string|null} [password]
 */

/**
 * @typedef {Object} UserOutput
 * @property {string} id
 * @property {string} username
 * @property {string} display_name
 * @property {string} status
 * @property {boolean} is_protected
 * @property {string|null} avatar_id
 */

/**
 * @typedef {Object} UserIdInput
 * @property {string} user_id
 */

/**
 * @typedef {Object} UserImportApplyInput
 * @property {string} preview_id
 * @property {string} confirmation_token
 */

/**
 * @typedef {Object} UserImportApplyOutput
 * @property {number} created
 * @property {number} updated
 * @property {number} suspended
 * @property {number} memberships_added
 * @property {number} memberships_removed
 */

/**
 * @typedef {Object} UserImportPreviewInput
 * @property {string|null} [file_id]
 * @property {Array<UserImportRecord>|null} [records]
 * @property {"upsert"|"sync"} [import_mode]
 * @property {string} external_source
 */

/**
 * @typedef {Object} UserImportPreviewOutput
 * @property {string} preview_id
 * @property {string} confirmation_token
 * @property {Array<string>} users_to_create
 * @property {Array<string>} users_to_update
 * @property {Array<string>} users_to_suspend
 * @property {Array<Object<string, string>>} memberships_to_add
 * @property {Array<Object<string, string>>} memberships_to_remove
 * @property {Array<string>} warnings
 */

/**
 * @typedef {Object} UserSearchInput
 * @property {string} query
 * @property {number} [limit]
 */

/**
 * @typedef {Object} UserSearchOutput
 * @property {Array<UserOutput>} users
 */

/**
 * @typedef {Object} UserSetPasswordInput
 * @property {string} user_id
 * @property {string} new_password
 */

/**
 * @typedef {Object} UserUpdateInput
 * @property {string} user_id
 * @property {string|null} [username]
 * @property {string|null} [display_name]
 */

/**
 * @typedef {Object} TestAccessListInput
 * @property {string} test_id
 */

/**
 * @typedef {Object} TestAccessOutput
 * @property {string} test_id
 * @property {Array<TestRoleConditionsOutput>} roles
 */

/**
 * @typedef {Object} TestAccessRemoveInput
 * @property {string} test_id
 * @property {Role} role
 */

/**
 * @typedef {Object} TestAccessSetInput
 * @property {string} test_id
 * @property {Role} role
 * @property {Array<Array<string>>} group_sets
 */

/**
 * @typedef {Object} AttemptAddTimeInput
 * @property {string} attempt_id
 * @property {number} seconds
 */

/**
 * @typedef {Object} TeacherAttemptOutput
 * @property {string} id
 * @property {string} test_id
 * @property {string} test_version_id
 * @property {string} user_id
 * @property {number} attempt_number
 * @property {string} status
 * @property {string} started_at
 * @property {string|null} expires_at
 * @property {string|null} submitted_at
 * @property {number|string} max_score
 * @property {number|string|null} auto_score
 * @property {number|string|null} manual_score
 * @property {number|string|null} final_score
 */

/**
 * @typedef {Object} AttemptAnswerUpdateInput
 * @property {string} attempt_id
 * @property {string} attempt_question_id
 * @property {number} expected_version
 * @property {object} answer
 */

/**
 * @typedef {Object} AttemptAnswerSavedOutput
 * @property {string} attempt_id
 * @property {string} attempt_question_id
 * @property {number} version
 * @property {string} updated_at
 */

/**
 * @typedef {Object} AttemptEventsAppendInput
 * @property {string} attempt_id
 * @property {Array<AttemptEventItem>} events
 */

/**
 * @typedef {Object} AttemptEventsAppendOutput
 * @property {number} accepted
 * @property {number} duplicates
 */

/**
 * @typedef {Object} AttemptIdInput
 * @property {string} attempt_id
 */

/**
 * @typedef {Object} TeacherAttemptDetailOutput
 * @property {string} id
 * @property {string} test_id
 * @property {string} test_version_id
 * @property {string} user_id
 * @property {number} attempt_number
 * @property {string} status
 * @property {string} started_at
 * @property {string|null} expires_at
 * @property {string|null} submitted_at
 * @property {number|string} max_score
 * @property {number|string|null} auto_score
 * @property {number|string|null} manual_score
 * @property {number|string|null} final_score
 * @property {Array<TeacherAttemptQuestionOutput>} questions
 */

/**
 * @typedef {Object} AttemptGradeInput
 * @property {string} attempt_id
 * @property {string} attempt_question_id
 * @property {number|string} manual_score
 */

/**
 * @typedef {Object} AttemptOpenOutput
 * @property {AttemptOutput} attempt
 * @property {Array<AttemptQuestionSummary>} questions
 */

/**
 * @typedef {Object} AttemptOverrideScoreInput
 * @property {string} attempt_id
 * @property {number|string} final_score
 * @property {string} reason
 */

/**
 * @typedef {Object} AttemptQuestionGetInput
 * @property {string} attempt_id
 * @property {string} attempt_question_id
 */

/**
 * @typedef {Object} AttemptQuestionOutput
 * @property {string} attempt_question_id
 * @property {number} position
 * @property {number|string} max_score
 * @property {object} content_snapshot
 * @property {string} answer_type
 * @property {object} answer_schema
 * @property {object|null} answer
 * @property {number} answer_version
 */

/**
 * @typedef {Object} AttemptResultOutput
 * @property {string} attempt_id
 * @property {string} status
 * @property {number|string} max_score
 * @property {number|string|null} [score]
 * @property {number|string|null} [percentage]
 * @property {Array<object>|null} [user_answers]
 * @property {Object<string, string>|null} [choice_labels]
 * @property {Array<object>|null} [correct_answers]
 * @property {Array<object>|null} [explanations]
 * @property {boolean} [requires_manual_review]
 */

/**
 * @typedef {Object} AttemptStartInput
 * @property {string} test_id
 */

/**
 * @typedef {Object} AttemptOutput
 * @property {string} id
 * @property {string} test_id
 * @property {string} test_version_id
 * @property {string} user_id
 * @property {number} attempt_number
 * @property {string} status
 * @property {string} started_at
 * @property {string|null} expires_at
 * @property {string|null} submitted_at
 * @property {number|string} max_score
 */

/**
 * @typedef {Object} AttemptTimelineOutput
 * @property {TeacherAttemptOutput} attempt
 * @property {Array<AttemptTimelineEventOutput>} events
 * @property {object} risk
 */

/**
 * @typedef {Object} TestCreateInput
 * @property {string} folder_id
 * @property {string} name
 * @property {string|null} [description]
 * @property {string|null} [icon_name]
 * @property {string|null} [order_key]
 */

/**
 * @typedef {Object} TestOutput
 * @property {string} id
 * @property {string} folder_id
 * @property {string} name
 * @property {string|null} description
 * @property {string|null} icon_name
 * @property {string} order_key
 * @property {string|null} published_version_id
 * @property {string} created_by
 */

/**
 * @typedef {Object} TestIdInput
 * @property {string} test_id
 */

/**
 * @typedef {Object} TestStudentOpenOutput
 * @property {TestOutput} test
 * @property {TestSettings} settings
 * @property {Array<AttemptOutput>} attempts
 */

/**
 * @typedef {Object} TestEditorOutput
 * @property {TestOutput} test
 * @property {Array<SlotOutput>} slots
 */

/**
 * @typedef {Object} TestOwnerTransferInput
 * @property {string} test_id
 * @property {string} target_group_id
 */

/**
 * @typedef {Object} TestPublishInput
 * @property {string} test_id
 * @property {TestSettings} [settings]
 */

/**
 * @typedef {Object} TestVersionOutput
 * @property {string} id
 * @property {string} test_id
 * @property {number} version_number
 * @property {object} settings
 * @property {object} snapshot
 * @property {string} created_by
 * @property {string} created_at
 */

/**
 * @typedef {Object} TestResultsListInput
 * @property {string} test_id
 * @property {number} [limit]
 */

/**
 * @typedef {Object} TestResultsListOutput
 * @property {Array<TeacherAttemptOutput>} attempts
 */

/**
 * @typedef {Object} SlotCreateInput
 * @property {string} test_id
 * @property {number} position
 * @property {string} bank_id
 * @property {number} questions_to_select
 * @property {number|string} max_score
 */

/**
 * @typedef {Object} SlotOutput
 * @property {string} id
 * @property {string} test_id
 * @property {number} position
 * @property {string} bank_id
 * @property {number} questions_to_select
 * @property {number|string} max_score
 */

/**
 * @typedef {Object} SlotIdInput
 * @property {string} slot_id
 */

/**
 * @typedef {Object} SlotMoveInput
 * @property {string} slot_id
 * @property {number} position
 */

/**
 * @typedef {Object} SlotUpdateInput
 * @property {string} slot_id
 * @property {string|null} [bank_id]
 * @property {number|null} [questions_to_select]
 * @property {number|string|null} [max_score]
 */

/**
 * @typedef {Object} TestUpdateInput
 * @property {string} test_id
 * @property {string|null} [name]
 * @property {string|null} [description]
 * @property {string|null} [icon_name]
 * @property {string|null} [order_key]
 */

/**
 * @typedef {Object} AvatarUploadInput
 * @property {"image/jpeg"|"image/png"|"image/webp"|"image/gif"} content_type
 * @property {string} data_base64
 */

/**
 * @typedef {Object} AvatarUploadOutput
 * @property {string} avatar_id
 * @property {string} content_type
 * @property {number} size
 * @property {string} sha256
 */

/**
 * @typedef {Object} UserDataGetInput
 */

/**
 * @typedef {Object} UserDataOutput
 * @property {object} data
 */

/**
 * @typedef {Object} UserDataSetInput
 * @property {object} data
 */

/**
 * @typedef {Object} PasswordChangeInput
 * @property {string} current_password
 * @property {string} new_password
 */

/**
 * @typedef {Object} PasswordChangeOutput
 * @property {boolean} [changed]
 */

/**
 * @typedef {Object} WorkspaceInput
 */

/**
 * @typedef {Object} AssignmentsOutput
 * @property {Array<AssignmentOutput>} assignments
 */

/**
 * @typedef {Object} WorkspaceResourcesOutput
 * @property {Array<WorkspaceResourceOutput>} resources
 */

/**
 * @typedef {Object} WorkspaceIndexOutput
 * @property {Array<WorkspaceIndexNode>} nodes
 * @property {string} version
 */

/**
 * @typedef {Object} WorkspaceResourceInput
 * @property {ResourceType} resource_type
 * @property {string} resource_id
 */

/**
 * @typedef {Object} WorkspacePinOutput
 * @property {WorkspaceResourceOutput} resource
 * @property {boolean} pinned
 */

/**
 * @typedef {Object} FolderChanged
 * @property {string} folder_id
 */

/**
 * @typedef {Object} FolderChildrenChanged
 * @property {string} folder_id
 */

/**
 * @typedef {Object} PageChanged
 * @property {string} page_id
 */

/**
 * @typedef {Object} BlockChanged
 * @property {string} page_id
 * @property {BlockOutput|null} [block]
 * @property {string} block_id
 * @property {string} event_type
 */

/**
 * @typedef {Object} ServerShutdown
 * @property {string} [reason]
 */

/**
 * @typedef {Object} TestChanged
 * @property {string} test_id
 */

/**
 * @typedef {Object} WorkspacePinsChanged
 * @property {string} user_id
 */

/** @typedef {"folder"|"page"|"test"} ResourceType */

/** @typedef {"owner"|"editor"|"hidden"|"controller"|"student"|"blocked"} Role */

/**
 * @typedef {Object} AccessGroupOutput
 * @property {string} id
 * @property {string} name
 */

/**
 * @typedef {Object} RoleGroupConditionsOutput
 * @property {Role} role
 * @property {Array<Array<AccessGroupOutput>>} group_sets
 */

/**
 * @typedef {Object} FolderChild
 * @property {string} id
 * @property {string} kind
 * @property {string} name
 * @property {string|null} icon_name
 * @property {string} order_key
 */

/** @typedef {"open"|"merged"|"closed"} ChangeRequestStatus */

/** @typedef {"added"|"modified"|"deleted"|"renamed"} ChangedFileStatus */

/**
 * @typedef {Object} ChangedFileSummary
 * @property {string} path
 * @property {string|null} [previous_path]
 * @property {ChangedFileStatus} status
 * @property {number|null} [additions]
 * @property {number|null} [deletions]
 * @property {string|null} [web_url]
 */

/** @typedef {"pending"|"success"|"failure"|"cancelled"|"unknown"} CiStatus */

/**
 * @typedef {Object} LabFileOutput
 * @property {string} file_id
 * @property {string} original_name
 * @property {string} content_type
 * @property {number} size
 */

/**
 * @typedef {Object} LabGitSnapshotOutput
 * @property {string} instance_id
 * @property {ProviderType} provider_type
 * @property {string} instance_web_url
 * @property {string|null} repository_external_id
 * @property {string} repository_full_name
 * @property {string|null} repository_clone_url
 * @property {string|null} repository_web_url
 * @property {string} branch
 * @property {string} commit_sha
 * @property {string|null} commit_web_url
 * @property {string|null} commit_message
 * @property {string|null} commit_author
 * @property {string|null} commit_author_login
 * @property {string|null} commit_author_avatar_url
 * @property {string|null} commit_authored_at
 * @property {Array<ChangedFileSummary>|null} commit_changed_files
 * @property {number|null} commit_changed_files_total
 * @property {boolean} commit_changed_files_truncated
 * @property {number|null} commit_additions
 * @property {number|null} commit_deletions
 * @property {string|null} change_request_external_id
 * @property {number|null} change_request_number
 * @property {string|null} change_request_url
 * @property {string|null} change_request_title
 * @property {ChangeRequestStatus|null} change_request_status
 * @property {CiStatus|null} ci_status
 * @property {string|null} ci_url
 * @property {string} captured_at
 */

/** @typedef {"github"|"gitlab"|"gitea"|"generic_git"} ProviderType */

/** @typedef {"files"|"git"} SourceType */

/** @typedef {"files"|"git"|"either"} SubmissionMode */

/**
 * @typedef {Object} LabRosterEntry
 * @property {string} user_id
 * @property {string} username
 * @property {string} display_name
 * @property {LabSubmissionOutput|null} submission
 */

/** @typedef {"unsupported_format"|"file_too_large"|"file_missing"|"document_rejected"|"timeout"|"service_unavailable"|"incompatible_report"|"processing_error"} FailureReason */

/**
 * @typedef {Object} NormocontrolCategoryOutput
 * @property {string} name
 * @property {Array<NormocontrolIssueOutput>} issues
 */

/**
 * @typedef {Object} NormocontrolIssueOutput
 * @property {string} location
 * @property {string} message
 */

/**
 * @typedef {Object} NormocontrolReportOutput
 * @property {number} total_issues
 * @property {boolean} is_clean
 * @property {Array<string>} notes
 * @property {Array<NormocontrolCategoryOutput>} categories
 * @property {object} raw
 * @property {string} checker_version
 * @property {string} schema_version
 * @property {string} norms_version
 * @property {string} computed_at
 */

/** @typedef {"draft"|"queued"|"running"|"done"|"failed"|"cancelled"} RunStatus */

/**
 * @typedef {Object} PageAccessGroupOutput
 * @property {string} id
 * @property {string} name
 */

/**
 * @typedef {Object} PageRoleGroupConditionsOutput
 * @property {Role} role
 * @property {Array<Array<PageAccessGroupOutput>>} group_sets
 */

/**
 * @typedef {Object} PageAuthorOutput
 * @property {string} id
 * @property {string} username
 * @property {string} display_name
 * @property {string|null} [avatar_id]
 */

/**
 * @typedef {Object} PageInputEventItem
 * @property {string} block_id
 * @property {"clipboard.paste"|"clipboard.copy"|"text.selected"|"provenance.requested"|"provenance.completed"} event_type
 * @property {number|null} [elapsed_ms]
 * @property {object} [payload]
 */

/**
 * @typedef {Object} PageResourceOutput
 * @property {string} file_id
 * @property {string} original_name
 * @property {string} content_type
 * @property {number} size
 * @property {string} sha256
 * @property {"download"|"preview"} behavior
 */

/**
 * @typedef {Object} BlockOpInput
 * @property {"create"|"update"|"delete"|"move"|"retype"} op
 * @property {string} block_id
 * @property {string|null} [block_type]
 * @property {*} [data]
 * @property {string|null} [zone]
 * @property {string|null} [order_key]
 * @property {number|null} [expected_block_version]
 */

/** @typedef {"connected"|"expired"|"disconnected"} ConnectionStatus */

/**
 * @typedef {Object} SourceControlConnectionOutput
 * @property {string} id
 * @property {string} instance_id
 * @property {ProviderType} provider_type
 * @property {string} username
 * @property {string|null} display_name
 * @property {string} connected_at
 * @property {string|null} token_expires_at
 * @property {ConnectionStatus} status
 */

/** @typedef {"oauth"|"repositories"|"branches"|"commits"|"commit_diff"|"change_requests"|"ci_status"|"webhooks"|"generic_remote_validation"} Capability */

/** @typedef {"https"|"ssh"} CloneProtocol */

/**
 * @typedef {Object} SourceControlInstanceOutput
 * @property {string} id
 * @property {string} display_name
 * @property {ProviderType} provider_type
 * @property {string} web_base_url
 * @property {Array<Capability>} capabilities
 * @property {Array<CloneProtocol>} allowed_clone_protocols
 * @property {boolean} enabled
 * @property {ConnectionStatus} connection_status
 */

/**
 * @typedef {Object} RepositorySummary
 * @property {string} instance_id
 * @property {string|null} [external_id]
 * @property {string} full_name
 * @property {string} name
 * @property {string|null} [owner_name]
 * @property {string|null} [web_url]
 * @property {string|null} [clone_url]
 * @property {string|null} [default_branch]
 * @property {boolean} [private]
 * @property {boolean} [can_read]
 * @property {string|null} [updated_at]
 */

/**
 * @typedef {Object} BranchSummary
 * @property {string} name
 * @property {string} commit_sha
 * @property {boolean|null} [protected]
 * @property {string|null} [web_url]
 */

/**
 * @typedef {Object} UserImportRecord
 * @property {string} external_id
 * @property {string} username
 * @property {string} display_name
 * @property {Array<string>} [memberships]
 */

/**
 * @typedef {Object} TestAccessGroupOutput
 * @property {string} id
 * @property {string} name
 */

/**
 * @typedef {Object} TestRoleConditionsOutput
 * @property {Role} role
 * @property {Array<Array<TestAccessGroupOutput>>} group_sets
 */

/**
 * @typedef {Object} AttemptEventItem
 * @property {string} local_id
 * @property {string|null} [attempt_question_id]
 * @property {"question.opened"|"question.closed"|"window.blur"|"window.focus"|"visibility.hidden"|"visibility.visible"|"text.selected"|"clipboard.copy"|"clipboard.cut"|"clipboard.paste"|"answer.changed"|"answer.cleared"|"typing.sample"|"challenge.issued"|"challenge.passed"|"challenge.failed"|"connection.lost"|"device.changed"|"attempt.started"|"attempt.paused"|"attempt.submitted"|"attempt.expired"} event_type
 * @property {number} client_elapsed_ms
 * @property {string|null} [client_timestamp]
 * @property {object} [payload]
 */

/**
 * @typedef {Object} TeacherAttemptQuestionOutput
 * @property {string} attempt_question_id
 * @property {string} question_id
 * @property {string} question_version_id
 * @property {number} position
 * @property {number|string} max_score
 * @property {object} content_snapshot
 * @property {string} answer_type
 * @property {object} answer_schema
 * @property {object} answer_key
 * @property {object} scoring_rules
 * @property {object|null} answer
 * @property {number} answer_version
 * @property {number|string|null} auto_score
 * @property {number|string|null} manual_score
 * @property {boolean} requires_manual_review
 * @property {string|null} updated_at
 * @property {string|null} submitted_at
 */

/**
 * @typedef {Object} AttemptQuestionSummary
 * @property {string} id
 * @property {number} position
 * @property {number|string} max_score
 * @property {number} answer_version
 */

/**
 * @typedef {Object} AttemptTimelineEventOutput
 * @property {number} sequence
 * @property {string} event_type
 * @property {string|null} attempt_question_id
 * @property {number} client_elapsed_ms
 * @property {string|null} client_timestamp
 * @property {string} server_received_at
 * @property {object} payload
 */

/**
 * @typedef {Object} ResultVisibility
 * @property {"never"|"after_attempt"|"after_all_attempts"|"after_test_close"|"manual"} [score]
 * @property {"never"|"after_attempt"|"after_all_attempts"|"after_test_close"|"manual"} [percentage]
 * @property {"never"|"after_attempt"|"after_all_attempts"|"after_test_close"|"manual"} [user_answers]
 * @property {"never"|"after_attempt"|"after_all_attempts"|"after_test_close"|"manual"} [correct_answers]
 * @property {"never"|"after_attempt"|"after_all_attempts"|"after_test_close"|"manual"} [answer_keys]
 * @property {"never"|"after_attempt"|"after_all_attempts"|"after_test_close"|"manual"} [explanations]
 */

/**
 * @typedef {Object} TestSettings
 * @property {number} [attempt_limit]
 * @property {number|null} [time_limit_seconds]
 * @property {boolean} [shuffle_slots]
 * @property {boolean} [shuffle_answers]
 * @property {string|null} [start_at]
 * @property {string|null} [close_at]
 * @property {ResultVisibility} [result_visibility]
 */

/**
 * @typedef {Object} AssignmentAttemptOutput
 * @property {string} id
 * @property {number} attempt_number
 * @property {string} status
 * @property {string} started_at
 * @property {string|null} submitted_at
 * @property {number|string} max_score
 * @property {number|string|null} score
 * @property {boolean|null} [passed]
 */

/**
 * @typedef {Object} AssignmentOutput
 * @property {"test"|"lab"} [kind]
 * @property {string} resource_id
 * @property {"score"|"pass_fail"|null} [grading]
 * @property {string} name
 * @property {string|null} description
 * @property {string} folder_id
 * @property {string} folder_name
 * @property {Role} effective_role
 * @property {boolean} published
 * @property {string|null} start_at
 * @property {string|null} close_at
 * @property {number} attempt_limit
 * @property {number|null} time_limit_seconds
 * @property {number} attempts_used
 * @property {Array<AssignmentAttemptOutput>} attempts
 */

/**
 * @typedef {Object} WorkspaceResourceOutput
 * @property {ResourceType} resource_type
 * @property {string} resource_id
 * @property {string} name
 * @property {string|null} icon_name
 * @property {Role} effective_role
 * @property {string|null} [created_at]
 */

/**
 * @typedef {Object} WorkspaceIndexNode
 * @property {string} id
 * @property {string} type
 * @property {string|null} parent_id
 * @property {string} kind
 * @property {string} name
 * @property {string|null} [icon_name]
 * @property {string} [order_key]
 * @property {string} role
 * @property {boolean|null} [published]
 */


/**
 * @typedef {Object} ActionCallOptions
 * @property {number} [timeout]
 * @property {string} [request_id]
 */

/**
 * @typedef {Object} ReconnectOptions
 * @property {boolean} [enabled]
 * @property {number} [initial_delay]
 * @property {number} [max_delay]
 * @property {number} [factor]
 * @property {number} [jitter]
 */

/**
 * @typedef {Object} ApiClientOptions
 * @property {string} url
 * @property {string|null} [token]
 * @property {number} [timeout]
 * @property {ReconnectOptions} [reconnect]
 */

/**
 * @typedef {Object} SyncEvent
 * @property {string} scope
 * @property {string} scope_id
 * @property {string} event_id
 * @property {number} sequence
 * @property {string|null} caused_by
 */

/**
 * @typedef {Object} ConnectionEvent
 * @property {string} connection_id
 */

/**
 * @typedef {Object} ReconnectEvent
 * @property {number} attempt
 */

/**
 * @typedef {Object} SequenceGapEvent
 * @property {string} scope
 * @property {string} scope_id
 * @property {number} expected
 * @property {number} received
 */

/**
 * @typedef {Object} ProtocolErrorPayload
 * @property {string} code
 * @property {string} message
 * @property {*} [data]
 */

/** @typedef {() => void} Unsubscribe */

/**
 * @template T
 * @callback SyncCallback
 * @param {T} data
 * @param {SyncEvent} event
 * @returns {void}
 */

export class ApiError extends Error {
  /**
   * @param {string} code
   * @param {string} message
   * @param {*} [data]
   * @param {string|null} [traceId]
   */
  constructor(code, message, data = null, traceId = null) {
    super(message); this.name = "ApiError"; this.code = code; this.data = data; this.traceId = traceId;
  }
}
export class RequestTimeoutError extends Error {
  /** @param {string} action */
  constructor(action) { super(`Action timed out: ${action}`); this.name = "RequestTimeoutError"; }
}
export class ConnectionClosedError extends Error {
  /** @param {string} [message] */
  constructor(message = "WebSocket connection closed") { super(message); this.name = "ConnectionClosedError"; }
}
export class ProtocolError extends Error {
  /** @param {ProtocolErrorPayload} error */
  constructor(error) { super(error.message); this.name = "ProtocolError"; this.code = error.code; this.data = error.data; }
}

/**
 * @param {ApiClientOptions} options
 * @returns {ApiClient}
 */
export function createApi(options) { return new ApiClient(options); }

export class ApiClient {
  /** @param {ApiClientOptions} options */
  constructor(options) {
    if (!options?.url) throw new TypeError("createApi requires a url");
    this.url = options.url; this.token = options.token ?? null;
    this.protocolVersion = 1; this.defaultTimeout = options.timeout ?? 30000;
    this.reconnect = { enabled: true, initial_delay: 500, max_delay: 15000, factor: 1.8, jitter: 0.2, ...(options.reconnect || {}) };
    this.socket = null; this.pendingRequests = new Map(); this.syncListeners = new Map();
    this.callbacks = new Map(); this.sequence = new Map();
    this.actions = {
      "access": {
        "user": {
          "invite": /**
           * access.user.invite
           * @param {UserInvitationInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<UserInvitationOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("access.user.invite", data, options),
          "remove": /**
           * access.user.remove
           * @param {UserInvitationInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<UserInvitationOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("access.user.remove", data, options)
        }
      },
      "chat": {
        "delete_message": /**
         * chat.delete_message
         * @param {MessageDeleteInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<MessageDeleted>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("chat.delete_message", data, options),
        "edit_message": /**
         * chat.edit_message
         * @param {MessageEditInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<MessageOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("chat.edit_message", data, options),
        "history": /**
         * chat.history
         * @param {ChatHistoryInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<ChatOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("chat.history", data, options),
        "open": /**
         * chat.open
         * @param {ChatOpenInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<ChatOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("chat.open", data, options),
        "send_message": /**
         * chat.send_message
         * @param {MessageSendInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<MessageOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("chat.send_message", data, options)
      },
      "folder": {
        "access": {
          "list": /**
           * folder.access.list
           * @param {FolderAccessListInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<FolderAccessOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("folder.access.list", data, options),
          "remove": /**
           * folder.access.remove
           * @param {FolderAccessRemoveInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<FolderAccessOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("folder.access.remove", data, options),
          "set": /**
           * folder.access.set
           * @param {FolderAccessSetInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<FolderAccessOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("folder.access.set", data, options)
        },
        "create": /**
         * folder.create
         * @param {FolderCreateInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<FolderOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("folder.create", data, options),
        "delete": /**
         * folder.delete
         * @param {FolderDeleteInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<FolderDeleteOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("folder.delete", data, options),
        "move": /**
         * folder.move
         * @param {FolderMoveInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<FolderOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("folder.move", data, options),
        "open": /**
         * folder.open
         * @param {FolderOpenInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<FolderOpenOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("folder.open", data, options),
        "owner": {
          "transfer": /**
           * folder.owner.transfer
           * @param {FolderOwnerTransferInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<FolderAccessOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("folder.owner.transfer", data, options)
        },
        "restore": /**
         * folder.restore
         * @param {FolderRestoreInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<FolderOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("folder.restore", data, options),
        "shortcut": {
          "create": /**
           * folder.shortcut.create
           * @param {FolderShortcutCreateInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<FolderOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("folder.shortcut.create", data, options),
          "delete": /**
           * folder.shortcut.delete
           * @param {FolderDeleteInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<FolderDeleteOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("folder.shortcut.delete", data, options),
          "update": /**
           * folder.shortcut.update
           * @param {FolderShortcutUpdateInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<FolderOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("folder.shortcut.update", data, options)
        },
        "update": /**
         * folder.update
         * @param {FolderUpdateInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<FolderOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("folder.update", data, options)
      },
      "lab": {
        "draft": /**
         * lab.draft
         * @param {LabPageInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<LabSubmissionOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("lab.draft", data, options),
        "git": {
          "preview": /**
           * lab.git.preview
           * @param {LabGitSelectionInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<LabGitPreviewOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("lab.git.preview", data, options),
          "submit": /**
           * lab.git.submit
           * @param {LabGitSubmitInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<LabSubmissionOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("lab.git.submit", data, options)
        },
        "open": /**
         * lab.open
         * @param {LabPageInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<LabStudentViewOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("lab.open", data, options),
        "roster": /**
         * lab.roster
         * @param {LabPageInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<LabRosterOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("lab.roster", data, options),
        "settings": {
          "get": /**
           * lab.settings.get
           * @param {LabPageInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<LabSettingsOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("lab.settings.get", data, options),
          "set": /**
           * lab.settings.set
           * @param {LabSettingsInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<LabSettingsOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("lab.settings.set", data, options)
        },
        "submission": {
          "grade": /**
           * lab.submission.grade
           * @param {LabGradeInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<LabSubmissionOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("lab.submission.grade", data, options),
          "return": /**
           * lab.submission.return
           * @param {LabReturnInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<LabSubmissionOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("lab.submission.return", data, options)
        },
        "submissions": {
          "history": /**
           * lab.submissions.history
           * @param {LabSubmissionHistoryInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<LabSubmissionHistoryOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("lab.submissions.history", data, options)
        },
        "submit": /**
         * lab.submit
         * @param {LabSubmitInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<LabSubmissionOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("lab.submit", data, options)
      },
      "normocontrol": {
        "open": /**
         * normocontrol.open
         * @param {NormocontrolPageInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<NormocontrolViewOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("normocontrol.open", data, options),
        "run": {
          "cancel": /**
           * normocontrol.run.cancel
           * @param {NormocontrolRunIdInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<NormocontrolRunOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("normocontrol.run.cancel", data, options),
          "create": /**
           * normocontrol.run.create
           * @param {NormocontrolPageInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<NormocontrolRunOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("normocontrol.run.create", data, options),
          "get": /**
           * normocontrol.run.get
           * @param {NormocontrolRunIdInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<NormocontrolRunOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("normocontrol.run.get", data, options),
          "submit": /**
           * normocontrol.run.submit
           * @param {NormocontrolSubmitInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<NormocontrolRunOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("normocontrol.run.submit", data, options)
        },
        "settings": {
          "get": /**
           * normocontrol.settings.get
           * @param {NormocontrolPageInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<NormocontrolSettingsOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("normocontrol.settings.get", data, options),
          "set": /**
           * normocontrol.settings.set
           * @param {NormocontrolSettingsInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<NormocontrolSettingsOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("normocontrol.settings.set", data, options)
        }
      },
      "page": {
        "access": {
          "list": /**
           * page.access.list
           * @param {PageAccessListInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<PageAccessOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.access.list", data, options),
          "remove": /**
           * page.access.remove
           * @param {PageAccessRemoveInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<PageAccessOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.access.remove", data, options),
          "set": /**
           * page.access.set
           * @param {PageAccessSetInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<PageAccessOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.access.set", data, options)
        },
        "block": {
          "change_zone": /**
           * page.block.change_zone
           * @param {BlockMoveInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<BlockOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.block.change_zone", data, options),
          "create": /**
           * page.block.create
           * @param {BlockCreateInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<BlockOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.block.create", data, options),
          "delete": /**
           * page.block.delete
           * @param {BlockDeleteInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<BlockDeleteOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.block.delete", data, options),
          "lock": {
            "acquire": /**
             * page.block.lock.acquire
             * @param {BlockLockInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<BlockLeaseOutput>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("page.block.lock.acquire", data, options),
            "release": /**
             * page.block.lock.release
             * @param {BlockLeaseInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<BlockLockChanged>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("page.block.lock.release", data, options),
            "renew": /**
             * page.block.lock.renew
             * @param {BlockLeaseInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<BlockLeaseOutput>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("page.block.lock.renew", data, options),
            "switch": /**
             * page.block.lock.switch
             * @param {BlockLockSwitchInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<BlockLeaseOutput>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("page.block.lock.switch", data, options)
          },
          "move": /**
           * page.block.move
           * @param {BlockMoveInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<BlockOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.block.move", data, options),
          "type": {
            "change": /**
             * page.block.type.change
             * @param {BlockTypeChangeInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<BlockOutput>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("page.block.type.change", data, options)
          },
          "update": /**
           * page.block.update
           * @param {BlockUpdateInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<BlockOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.block.update", data, options)
        },
        "create": /**
         * page.create
         * @param {PageCreateInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<PageOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("page.create", data, options),
        "delete": /**
         * page.delete
         * @param {PageDeleteInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<PageDeleteOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("page.delete", data, options),
        "input_events": {
          "append": /**
           * page.input_events.append
           * @param {PageInputEventsAppendInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<PageInputEventsAppendOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.input_events.append", data, options)
        },
        "open": /**
         * page.open
         * @param {PageOpenInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<PageOpenOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("page.open", data, options),
        "owner": {
          "transfer": /**
           * page.owner.transfer
           * @param {PageOwnerTransferInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<PageAccessOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.owner.transfer", data, options)
        },
        "provenance": {
          "mark": /**
           * page.provenance.mark
           * @param {ProvenanceMarkInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<ProvenanceOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.provenance.mark", data, options),
          "remove": /**
           * page.provenance.remove
           * @param {ProvenanceRemoveInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<ProvenanceRemoveOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.provenance.remove", data, options),
          "report": /**
           * page.provenance.report
           * @param {ProvenanceReportInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<ProvenanceReportOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.provenance.report", data, options)
        },
        "publish": /**
         * page.publish
         * @param {PagePublishInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<RevisionOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("page.publish", data, options),
        "restore": /**
         * page.restore
         * @param {PageRestoreInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<PageOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("page.restore", data, options),
        "revision": {
          "create": /**
           * page.revision.create
           * @param {RevisionCreateInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<RevisionOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.revision.create", data, options),
          "get": /**
           * page.revision.get
           * @param {RevisionGetInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<RevisionOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.revision.get", data, options),
          "list": /**
           * page.revision.list
           * @param {RevisionListInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<RevisionListOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.revision.list", data, options),
          "restore": /**
           * page.revision.restore
           * @param {RevisionRestoreInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<RevisionOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.revision.restore", data, options)
        },
        "share": {
          "create": /**
           * page.share.create
           * @param {RevisionShareCreateInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<RevisionShareCreatedOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.share.create", data, options),
          "list": /**
           * page.share.list
           * @param {RevisionShareListInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<RevisionShareListOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.share.list", data, options),
          "revoke": /**
           * page.share.revoke
           * @param {RevisionShareRevokeInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<RevisionShareOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.share.revoke", data, options),
          "update": /**
           * page.share.update
           * @param {RevisionShareUpdateInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<RevisionShareOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.share.update", data, options)
        },
        "source": {
          "create": /**
           * page.source.create
           * @param {SourceCreateInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<SourceOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.source.create", data, options),
          "delete": /**
           * page.source.delete
           * @param {SourceDeleteInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<SourceDeleteOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.source.delete", data, options),
          "list": /**
           * page.source.list
           * @param {SourceListInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<SourceListOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.source.list", data, options),
          "update": /**
           * page.source.update
           * @param {SourceUpdateInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<SourceOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.source.update", data, options)
        },
        "transaction": {
          "apply": /**
           * page.transaction.apply
           * @param {PageTransactionInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<PageTransactionOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("page.transaction.apply", data, options)
        },
        "unpublish": /**
         * page.unpublish
         * @param {PagePublishInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<PageOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("page.unpublish", data, options),
        "update": /**
         * page.update
         * @param {PageUpdateInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<PageOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("page.update", data, options)
      },
      "question": {
        "create": /**
         * question.create
         * @param {QuestionCreateInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<QuestionOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("question.create", data, options),
        "open_editor": /**
         * question.open_editor
         * @param {QuestionOpenInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<QuestionEditorOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("question.open_editor", data, options),
        "publish_version": /**
         * question.publish_version
         * @param {QuestionVersionIdInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<QuestionVersionOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("question.publish_version", data, options),
        "save": /**
         * question.save
         * @param {QuestionSaveInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<QuestionVersionOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("question.save", data, options)
      },
      "question_bank": {
        "create": /**
         * question_bank.create
         * @param {QuestionBankCreateInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<QuestionBankOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("question_bank.create", data, options),
        "delete": /**
         * question_bank.delete
         * @param {QuestionBankIdInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<DeletedOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("question_bank.delete", data, options),
        "list": /**
         * question_bank.list
         * @param {QuestionBankListInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<QuestionBankListOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("question_bank.list", data, options),
        "update": /**
         * question_bank.update
         * @param {QuestionBankUpdateInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<QuestionBankOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("question_bank.update", data, options)
      },
      "source_control": {
        "connection": {
          "disconnect": /**
           * source_control.connection.disconnect
           * @param {ConnectionDisconnectInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<ConnectionDisconnectOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("source_control.connection.disconnect", data, options)
        },
        "connections": {
          "list": /**
           * source_control.connections.list
           * @param {InstanceListInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<ConnectionListOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("source_control.connections.list", data, options)
        },
        "instances": {
          "list": /**
           * source_control.instances.list
           * @param {InstanceListInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<InstanceListOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("source_control.instances.list", data, options)
        },
        "repositories": {
          "list": /**
           * source_control.repositories.list
           * @param {RepositoryListInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<RepositoryPageOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("source_control.repositories.list", data, options)
        },
        "repository": {
          "branches": /**
           * source_control.repository.branches
           * @param {RepositoryBranchesInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<BranchPageOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("source_control.repository.branches", data, options),
          "change_requests": {
            "get": /**
             * source_control.repository.change_requests.get
             * @param {ChangeRequestGetInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<ChangeRequestSummary>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("source_control.repository.change_requests.get", data, options),
            "list": /**
             * source_control.repository.change_requests.list
             * @param {ChangeRequestListInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<ChangeRequestPageOutput>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("source_control.repository.change_requests.list", data, options)
          },
          "ci": {
            "get": /**
             * source_control.repository.ci.get
             * @param {CiStatusInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<CheckSummary>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("source_control.repository.ci.get", data, options)
          },
          "commits": {
            "get": /**
             * source_control.repository.commits.get
             * @param {RepositoryCommitInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<CommitSummary>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("source_control.repository.commits.get", data, options)
          }
        }
      },
      "system": {
        "groups": {
          "create": /**
           * system.groups.create
           * @param {GroupCreateInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<GroupOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("system.groups.create", data, options),
          "delete": /**
           * system.groups.delete
           * @param {GroupIdInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<GroupDeletedOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("system.groups.delete", data, options),
          "get": /**
           * system.groups.get
           * @param {GroupIdInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<GroupOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("system.groups.get", data, options),
          "search": /**
           * system.groups.search
           * @param {GroupSearchInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<GroupSearchOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("system.groups.search", data, options),
          "update": /**
           * system.groups.update
           * @param {GroupUpdateInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<GroupOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("system.groups.update", data, options)
        },
        "memberships": {
          "add": /**
           * system.memberships.add
           * @param {MembershipChangeInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<MembershipOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("system.memberships.add", data, options),
          "bulk_set": /**
           * system.memberships.bulk_set
           * @param {MembershipBulkSetInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<MembershipOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("system.memberships.bulk_set", data, options),
          "list": /**
           * system.memberships.list
           * @param {MembershipListInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<MembershipOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("system.memberships.list", data, options),
          "remove": /**
           * system.memberships.remove
           * @param {MembershipChangeInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<MembershipOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("system.memberships.remove", data, options)
        },
        "source_control": {
          "instances": {
            "create": /**
             * system.source_control.instances.create
             * @param {InstanceCreateInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<SourceControlInstanceAdminOutput>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("system.source_control.instances.create", data, options),
            "disable": /**
             * system.source_control.instances.disable
             * @param {InstanceDisableInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<SourceControlInstanceAdminOutput>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("system.source_control.instances.disable", data, options),
            "list": /**
             * system.source_control.instances.list
             * @param {InstanceListInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<InstanceAdminListOutput>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("system.source_control.instances.list", data, options),
            "update": /**
             * system.source_control.instances.update
             * @param {InstanceUpdateInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<SourceControlInstanceAdminOutput>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("system.source_control.instances.update", data, options)
          }
        },
        "users": {
          "create": /**
           * system.users.create
           * @param {UserCreateInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<UserOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("system.users.create", data, options),
          "get": /**
           * system.users.get
           * @param {UserIdInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<UserOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("system.users.get", data, options),
          "import": {
            "apply": /**
             * system.users.import.apply
             * @param {UserImportApplyInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<UserImportApplyOutput>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("system.users.import.apply", data, options),
            "preview": /**
             * system.users.import.preview
             * @param {UserImportPreviewInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<UserImportPreviewOutput>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("system.users.import.preview", data, options)
          },
          "restore": /**
           * system.users.restore
           * @param {UserIdInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<UserOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("system.users.restore", data, options),
          "search": /**
           * system.users.search
           * @param {UserSearchInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<UserSearchOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("system.users.search", data, options),
          "set_password": /**
           * system.users.set_password
           * @param {UserSetPasswordInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<UserOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("system.users.set_password", data, options),
          "suspend": /**
           * system.users.suspend
           * @param {UserIdInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<UserOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("system.users.suspend", data, options),
          "update": /**
           * system.users.update
           * @param {UserUpdateInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<UserOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("system.users.update", data, options)
        }
      },
      "test": {
        "access": {
          "list": /**
           * test.access.list
           * @param {TestAccessListInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<TestAccessOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.access.list", data, options),
          "remove": /**
           * test.access.remove
           * @param {TestAccessRemoveInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<TestAccessOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.access.remove", data, options),
          "set": /**
           * test.access.set
           * @param {TestAccessSetInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<TestAccessOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.access.set", data, options)
        },
        "attempt": {
          "add_time": /**
           * test.attempt.add_time
           * @param {AttemptAddTimeInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<TeacherAttemptOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.attempt.add_time", data, options),
          "answer": {
            "update": /**
             * test.attempt.answer.update
             * @param {AttemptAnswerUpdateInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<AttemptAnswerSavedOutput>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("test.attempt.answer.update", data, options)
          },
          "events": {
            "append": /**
             * test.attempt.events.append
             * @param {AttemptEventsAppendInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<AttemptEventsAppendOutput>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("test.attempt.events.append", data, options)
          },
          "get": /**
           * test.attempt.get
           * @param {AttemptIdInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<TeacherAttemptDetailOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.attempt.get", data, options),
          "grade": /**
           * test.attempt.grade
           * @param {AttemptGradeInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<TeacherAttemptOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.attempt.grade", data, options),
          "open": /**
           * test.attempt.open
           * @param {AttemptIdInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<AttemptOpenOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.attempt.open", data, options),
          "override_score": /**
           * test.attempt.override_score
           * @param {AttemptOverrideScoreInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<TeacherAttemptOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.attempt.override_score", data, options),
          "pause": /**
           * test.attempt.pause
           * @param {AttemptIdInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<TeacherAttemptOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.attempt.pause", data, options),
          "question": {
            "get": /**
             * test.attempt.question.get
             * @param {AttemptQuestionGetInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<AttemptQuestionOutput>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("test.attempt.question.get", data, options)
          },
          "reset": /**
           * test.attempt.reset
           * @param {AttemptIdInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<TeacherAttemptOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.attempt.reset", data, options),
          "result": {
            "get": /**
             * test.attempt.result.get
             * @param {AttemptIdInput} data
             * @param {ActionCallOptions} [options]
             * @returns {Promise<AttemptResultOutput>}
             * @throws {ApiError}
             */
            (data, options = {}) => this.call("test.attempt.result.get", data, options)
          },
          "resume": /**
           * test.attempt.resume
           * @param {AttemptIdInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<TeacherAttemptOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.attempt.resume", data, options),
          "start": /**
           * test.attempt.start
           * @param {AttemptStartInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<AttemptOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.attempt.start", data, options),
          "submit": /**
           * test.attempt.submit
           * @param {AttemptIdInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<AttemptOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.attempt.submit", data, options),
          "timeline": /**
           * test.attempt.timeline
           * @param {AttemptIdInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<AttemptTimelineOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.attempt.timeline", data, options)
        },
        "create": /**
         * test.create
         * @param {TestCreateInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<TestOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("test.create", data, options),
        "delete": /**
         * test.delete
         * @param {TestIdInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<DeletedOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("test.delete", data, options),
        "open": /**
         * test.open
         * @param {TestIdInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<TestStudentOpenOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("test.open", data, options),
        "open_editor": /**
         * test.open_editor
         * @param {TestIdInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<TestEditorOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("test.open_editor", data, options),
        "owner": {
          "transfer": /**
           * test.owner.transfer
           * @param {TestOwnerTransferInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<TestAccessOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.owner.transfer", data, options)
        },
        "publish": /**
         * test.publish
         * @param {TestPublishInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<TestVersionOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("test.publish", data, options),
        "results": {
          "list": /**
           * test.results.list
           * @param {TestResultsListInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<TestResultsListOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.results.list", data, options)
        },
        "slot": {
          "create": /**
           * test.slot.create
           * @param {SlotCreateInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<SlotOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.slot.create", data, options),
          "delete": /**
           * test.slot.delete
           * @param {SlotIdInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<DeletedOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.slot.delete", data, options),
          "move": /**
           * test.slot.move
           * @param {SlotMoveInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<SlotOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.slot.move", data, options),
          "update": /**
           * test.slot.update
           * @param {SlotUpdateInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<SlotOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("test.slot.update", data, options)
        },
        "unpublish": /**
         * test.unpublish
         * @param {TestIdInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<TestOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("test.unpublish", data, options),
        "update": /**
         * test.update
         * @param {TestUpdateInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<TestOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("test.update", data, options)
      },
      "user": {
        "avatar": {
          "upload": /**
           * user.avatar.upload
           * @param {AvatarUploadInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<AvatarUploadOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("user.avatar.upload", data, options)
        },
        "data": {
          "get": /**
           * user.data.get
           * @param {UserDataGetInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<UserDataOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("user.data.get", data, options),
          "set": /**
           * user.data.set
           * @param {UserDataSetInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<UserDataOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("user.data.set", data, options)
        },
        "password": {
          "change": /**
           * user.password.change
           * @param {PasswordChangeInput} data
           * @param {ActionCallOptions} [options]
           * @returns {Promise<PasswordChangeOutput>}
           * @throws {ApiError}
           */
          (data, options = {}) => this.call("user.password.change", data, options)
        }
      },
      "workspace": {
        "assignments": /**
         * workspace.assignments
         * @param {WorkspaceInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<AssignmentsOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("workspace.assignments", data, options),
        "entrypoints": /**
         * workspace.entrypoints
         * @param {WorkspaceInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<WorkspaceResourcesOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("workspace.entrypoints", data, options),
        "index": /**
         * workspace.index
         * @param {WorkspaceInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<WorkspaceIndexOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("workspace.index", data, options),
        "pin": /**
         * workspace.pin
         * @param {WorkspaceResourceInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<WorkspacePinOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("workspace.pin", data, options),
        "pinned": /**
         * workspace.pinned
         * @param {WorkspaceInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<WorkspaceResourcesOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("workspace.pinned", data, options),
        "shared_with_me": /**
         * workspace.shared_with_me
         * @param {WorkspaceInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<WorkspaceResourcesOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("workspace.shared_with_me", data, options),
        "unpin": /**
         * workspace.unpin
         * @param {WorkspaceResourceInput} data
         * @param {ActionCallOptions} [options]
         * @returns {Promise<WorkspacePinOutput>}
         * @throws {ApiError}
         */
        (data, options = {}) => this.call("workspace.unpin", data, options)
      }
    };
    this.sync = {
      "chat": {
        "message_deleted": /**
         * chat.message_deleted
         * @param {string} scope_id
         * @param {SyncCallback<MessageDeleted>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("chat.message_deleted", scope_id, callback),
        "message_updated": /**
         * chat.message_updated
         * @param {string} scope_id
         * @param {SyncCallback<MessageOutput>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("chat.message_updated", scope_id, callback),
        "new_message": /**
         * chat.new_message
         * @param {string} scope_id
         * @param {SyncCallback<MessageOutput>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("chat.new_message", scope_id, callback)
      },
      "folder": {
        "access_changed": /**
         * folder.access_changed
         * @param {string} scope_id
         * @param {SyncCallback<FolderChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("folder.access_changed", scope_id, callback),
        "children_changed": /**
         * folder.children_changed
         * @param {string} scope_id
         * @param {SyncCallback<FolderChildrenChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("folder.children_changed", scope_id, callback),
        "deleted": /**
         * folder.deleted
         * @param {string} scope_id
         * @param {SyncCallback<FolderChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("folder.deleted", scope_id, callback),
        "metadata_changed": /**
         * folder.metadata_changed
         * @param {string} scope_id
         * @param {SyncCallback<FolderChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("folder.metadata_changed", scope_id, callback)
      },
      "normocontrol": {
        "run": {
          "changed": /**
           * normocontrol.run.changed
           * @param {string} scope_id
           * @param {SyncCallback<NormocontrolRunOutput>} callback
           * @returns {Unsubscribe}
           */
          (scope_id, callback) => this._subscribeSync("normocontrol.run.changed", scope_id, callback)
        }
      },
      "page": {
        "access_changed": /**
         * page.access_changed
         * @param {string} scope_id
         * @param {SyncCallback<PageChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("page.access_changed", scope_id, callback),
        "block": {
          "lock_changed": /**
           * page.block.lock_changed
           * @param {string} scope_id
           * @param {SyncCallback<BlockLockChanged>} callback
           * @returns {Unsubscribe}
           */
          (scope_id, callback) => this._subscribeSync("page.block.lock_changed", scope_id, callback)
        },
        "block_changed": /**
         * page.block_changed
         * @param {string} scope_id
         * @param {SyncCallback<BlockChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("page.block_changed", scope_id, callback),
        "deleted": /**
         * page.deleted
         * @param {string} scope_id
         * @param {SyncCallback<PageChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("page.deleted", scope_id, callback),
        "metadata_changed": /**
         * page.metadata_changed
         * @param {string} scope_id
         * @param {SyncCallback<PageChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("page.metadata_changed", scope_id, callback),
        "provenance_changed": /**
         * page.provenance_changed
         * @param {string} scope_id
         * @param {SyncCallback<PageChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("page.provenance_changed", scope_id, callback),
        "provenance_report_created": /**
         * page.provenance_report_created
         * @param {string} scope_id
         * @param {SyncCallback<PageChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("page.provenance_report_created", scope_id, callback),
        "published": /**
         * page.published
         * @param {string} scope_id
         * @param {SyncCallback<PageChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("page.published", scope_id, callback),
        "revision_created": /**
         * page.revision_created
         * @param {string} scope_id
         * @param {SyncCallback<PageChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("page.revision_created", scope_id, callback),
        "sources_changed": /**
         * page.sources_changed
         * @param {string} scope_id
         * @param {SyncCallback<PageChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("page.sources_changed", scope_id, callback),
        "structure_changed": /**
         * page.structure_changed
         * @param {string} scope_id
         * @param {SyncCallback<PageChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("page.structure_changed", scope_id, callback)
      },
      "server": {
        "shutdown": /**
         * server.shutdown
         * @param {string} scope_id
         * @param {SyncCallback<ServerShutdown>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("server.shutdown", scope_id, callback)
      },
      "test": {
        "attempt": {
          "answer_saved": /**
           * test.attempt.answer_saved
           * @param {string} scope_id
           * @param {SyncCallback<AttemptAnswerSavedOutput>} callback
           * @returns {Unsubscribe}
           */
          (scope_id, callback) => this._subscribeSync("test.attempt.answer_saved", scope_id, callback),
          "graded": /**
           * test.attempt.graded
           * @param {string} scope_id
           * @param {SyncCallback<AttemptOutput>} callback
           * @returns {Unsubscribe}
           */
          (scope_id, callback) => this._subscribeSync("test.attempt.graded", scope_id, callback),
          "state_changed": /**
           * test.attempt.state_changed
           * @param {string} scope_id
           * @param {SyncCallback<AttemptOutput>} callback
           * @returns {Unsubscribe}
           */
          (scope_id, callback) => this._subscribeSync("test.attempt.state_changed", scope_id, callback),
          "time_changed": /**
           * test.attempt.time_changed
           * @param {string} scope_id
           * @param {SyncCallback<AttemptOutput>} callback
           * @returns {Unsubscribe}
           */
          (scope_id, callback) => this._subscribeSync("test.attempt.time_changed", scope_id, callback)
        },
        "metadata_changed": /**
         * test.metadata_changed
         * @param {string} scope_id
         * @param {SyncCallback<TestChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("test.metadata_changed", scope_id, callback),
        "published": /**
         * test.published
         * @param {string} scope_id
         * @param {SyncCallback<TestChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("test.published", scope_id, callback),
        "results_changed": /**
         * test.results_changed
         * @param {string} scope_id
         * @param {SyncCallback<AttemptOutput>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("test.results_changed", scope_id, callback)
      },
      "workspace": {
        "pins_changed": /**
         * workspace.pins_changed
         * @param {string} scope_id
         * @param {SyncCallback<WorkspacePinsChanged>} callback
         * @returns {Unsubscribe}
         */
        (scope_id, callback) => this._subscribeSync("workspace.pins_changed", scope_id, callback)
      }
    };
    /** @type {string|null} */
    this.connectionId = null;
    this._connectPromise = null; this._manualClose = false; this._reconnectAttempt = 0; this._reconnectTimer = null;
  }
  /** @returns {Promise<void>} */
  async connect() {
    this._manualClose = false;
    if (this.socket?.readyState === WebSocket.OPEN) return;
    if (this._connectPromise) return this._connectPromise;
    this._connectPromise = this._open();
    try { await this._connectPromise; } finally { this._connectPromise = null; }
  }
  /** @returns {Promise<void>} */
  _open() {
    return new Promise((resolve, reject) => {
      const socket = new WebSocket(this.url); this.socket = socket; let settled = false;
      const fail = (error) => { if (!settled) { settled = true; reject(error); } };
      socket.onopen = () => socket.send(JSON.stringify({ v: this.protocolVersion, type: "hello", token: this.token, client: { name: "api.js", version: "0.1.0" } }));
      socket.onmessage = (event) => {
        let message; try { message = JSON.parse(event.data); } catch (_) { return this._emit("protocol_error", new ProtocolError({code:"invalid_json",message:"Server sent invalid JSON"})); }
        if (message.type === "hello_result") {
          // PATCHED (not from codegen): browsers only allow client-initiated
          // WebSocket.close() with code 1000 or 3000-4999 — 1002 throws
          // InvalidAccessError. Report this to whoever owns the wsapi
          // generator so the fix lands upstream instead of drifting here.
          if (!message.ok) {
            // PATCHED (not from codegen): an `authentication_failed` hello is
            // terminal — the token is invalid or expired, and reconnecting
            // replays the same dead token forever. Suppress the retry loop and
            // let `auth_failed` listeners refresh the token or log out.
            const error = new ProtocolError(message.error); fail(error);
            if (message.error?.code === "authentication_failed") { this._manualClose = true; this._emit("auth_failed", error); }
            socket.close(4000, "hello rejected"); return;
          }
          settled = true; this.connectionId = message.connection_id; this._reconnectAttempt = 0; resolve(); this._emit("connect", { connection_id: this.connectionId }); return;
        }
        this._handleMessage(message);
      };
      socket.onerror = () => fail(new ConnectionClosedError("WebSocket connection failed"));
      socket.onclose = (event) => { fail(new ConnectionClosedError(event.reason)); this._handleClose(event); };
    });
  }
  /**
   * @param {number} [code]
   * @param {string} [reason]
   * @returns {Promise<void>}
   */
  async close(code = 1000, reason = "client close") {
    this._manualClose = true; clearTimeout(this._reconnectTimer); this._reconnectTimer = null;
    if (this.socket && this.socket.readyState < WebSocket.CLOSING) this.socket.close(code, reason);
    this._rejectPending(new ConnectionClosedError(reason)); this.socket = null;
  }
  /** @returns {Promise<void>} */
  /** PATCHED (not from codegen): подписка на область целиком, без разбора по scope_id.
   * Нужна индексу дерева: он следит за изменениями во всех видимых папках сразу, а
   * подписываться поимённо на тысячу идентификаторов — тысяча слушателей на одно и то же
   * действие. @param {string} scope @param {(data: *, event: *) => void} callback @returns {Unsubscribe} */
  on_scope(scope, callback) {
    const listener = { callback, scopeId: null };
    let listeners = this.syncListeners.get(scope); if (!listeners) this.syncListeners.set(scope, listeners = new Set());
    listeners.add(listener); return () => { listeners.delete(listener); if (!listeners.size) this.syncListeners.delete(scope); };
  }
  waitUntilConnected() { return this.socket?.readyState === WebSocket.OPEN ? Promise.resolve() : this.connect(); }
  /**
   * @template T
   * @param {string} action
   * @param {*} data
   * @param {ActionCallOptions} [options]
   * @returns {Promise<T>}
   */
  async call(action, data, options = {}) {
    await this.waitUntilConnected();
    const requestId = options.request_id || (globalThis.crypto?.randomUUID?.() ?? `${Date.now()}-${Math.random()}`);
    const message = { v: this.protocolVersion, type: "action", request_id: requestId, action, data };
    return new Promise((resolve, reject) => {
      const timeout = setTimeout(() => { this.pendingRequests.delete(requestId); reject(new RequestTimeoutError(action)); }, options.timeout ?? this.defaultTimeout);
      this.pendingRequests.set(requestId, { action, resolve, reject, timeout });
      try { this.socket.send(JSON.stringify(message)); } catch (error) { clearTimeout(timeout); this.pendingRequests.delete(requestId); reject(error); }
    });
  }
  /**
   * @template T
   * @param {string} scope
   * @param {string} scope_id
   * @param {SyncCallback<T>} callback
   * @returns {Unsubscribe}
   */
  _subscribeSync(scope, scope_id, callback) {
    if (typeof scope_id !== "string") throw new TypeError("sync scope_id must be a string");
    if (typeof callback !== "function") throw new TypeError("sync callback must be a function");
    const listener = { callback, scopeId: scope_id };
    let listeners = this.syncListeners.get(scope); if (!listeners) this.syncListeners.set(scope, listeners = new Set());
    listeners.add(listener); return () => { listeners.delete(listener); if (!listeners.size) this.syncListeners.delete(scope); };
  }
  /** @param {(event: ConnectionEvent) => void} cb @returns {Unsubscribe} */
  on_connect(cb) { return this._on("connect", cb); }
  /** @param {(event: CloseEvent) => void} cb @returns {Unsubscribe} */
  on_disconnect(cb) { return this._on("disconnect", cb); }
  /** @param {(event: ReconnectEvent) => void} cb @returns {Unsubscribe} */
  on_reconnect(cb) { return this._on("reconnect", cb); }
  /** @param {(error: ProtocolError) => void} cb @returns {Unsubscribe} */
  on_protocol_error(cb) { return this._on("protocol_error", cb); }
  /** PATCHED (not from codegen): fires when the server rejects `hello` with `authentication_failed`. Reconnection is already suppressed; the listener owns the recovery (refresh the token, then `connect()` again — or log out). @param {(error: ProtocolError) => void} cb @returns {Unsubscribe} */
  on_auth_failed(cb) { return this._on("auth_failed", cb); }
  /** @param {(event: SequenceGapEvent) => void} cb @returns {Unsubscribe} */
  on_sequence_gap(cb) { return this._on("sequence_gap", cb); }
  /** @param {string} name @param {(value: *) => void} cb @returns {Unsubscribe} */
  _on(name, cb) { let set = this.callbacks.get(name); if (!set) this.callbacks.set(name, set = new Set()); set.add(cb); return () => set.delete(cb); }
  /** @param {string} name @param {*} value @returns {void} */
  _emit(name, value) { for (const cb of this.callbacks.get(name) || []) { try { cb(value); } catch (error) { queueMicrotask(() => { throw error; }); } } }
  /** @param {*} message @returns {void} */
  _handleMessage(message) {
    if (message.v !== this.protocolVersion) return this._emit("protocol_error", new ProtocolError({ code: "invalid_protocol_version", message: `Got protocol v${message.v}` }));
    if (message.type === "action_result") {
      const pending = this.pendingRequests.get(message.request_id); if (!pending) return;
      clearTimeout(pending.timeout); this.pendingRequests.delete(message.request_id);
      message.ok ? pending.resolve(message.data) : pending.reject(new ApiError(message.error.code, message.error.message, message.error.data, message.error.trace_id));
    } else if (message.type === "sync") this._handleSync(message);
    else if (message.type === "ping" && this.socket?.readyState === WebSocket.OPEN) this.socket.send(JSON.stringify({ v: this.protocolVersion, type: "pong", timestamp: message.timestamp }));
    else if (message.type === "protocol_error") this._emit("protocol_error", new ProtocolError(message.error));
  }
  /** @param {*} message @returns {void} */
  _handleSync(message) {
    const key = `${message.scope}\u0000${message.scope_id}`, previous = this.sequence.get(key);
    if (previous != null && message.sequence !== previous + 1) this._emit("sequence_gap", { scope: message.scope, scope_id: message.scope_id, expected: previous + 1, received: message.sequence });
    this.sequence.set(key, message.sequence);
    const event = { scope: message.scope, scope_id: message.scope_id, event_id: message.event_id, sequence: message.sequence, caused_by: message.caused_by ?? null };
    for (const listener of this.syncListeners.get(message.scope) || []) if (listener.scopeId === null || listener.scopeId === message.scope_id) listener.callback(message.data, event);
  }
  /** @param {CloseEvent} event @returns {void} */
  _handleClose(event) {
    this.socket = null; this._rejectPending(new ConnectionClosedError(event.reason)); this._emit("disconnect", event);
    if (!this._manualClose && this.reconnect.enabled) this._scheduleReconnect();
  }
  /** @param {Error} error @returns {void} */
  _rejectPending(error) { for (const pending of this.pendingRequests.values()) { clearTimeout(pending.timeout); pending.reject(error); } this.pendingRequests.clear(); }
  /** @returns {void} */
  _scheduleReconnect() {
    if (this._reconnectTimer) return;
    const base = Math.min(this.reconnect.max_delay, this.reconnect.initial_delay * Math.pow(this.reconnect.factor, this._reconnectAttempt++));
    const delay = Math.max(0, base * (1 + (Math.random() * 2 - 1) * this.reconnect.jitter));
    this._reconnectTimer = setTimeout(async () => { this._reconnectTimer = null; try { await this.connect(); this._emit("reconnect", { attempt: this._reconnectAttempt }); } catch (_) { if (!this._manualClose) this._scheduleReconnect(); } }, delay);
  }
}
